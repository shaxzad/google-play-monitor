/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    esmExternals: true,
  },
  outputFileTracingRoot: '/Users/coder/Documents/google paly scraper/google-play-monitor',
};

module.exports = nextConfig;
